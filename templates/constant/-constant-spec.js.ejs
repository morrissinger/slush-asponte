/*jshint -W030 */
describe('<%= moduleNameVar %> module', () => {

	/**
	 * Add any additional variables you want to declare on the top scope for this test suite.
	 */
	var <%= constantNameVar %>;

	beforeEach((done) => {

		/**
		 * Add any additional test initialization routines to run before each test.
		 */
		module('<%= moduleNameVar %>');
		inject((<%= constantNameVar %>_) => {
			<%= constantNameVar %> = _<%= constantNameVar %>_;
			done();
		});
	});

	describe('<%= constantNameVar %> constant', () => {

	});

});