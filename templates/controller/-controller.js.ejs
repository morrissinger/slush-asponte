/**
 * <%= controllerName %> Controller.
 */
(function () {

	class Controller {

		constructor(<%= injections.formatted %>) {}<% if (methods.list.length > 0) { %>
		<% _.each(methods.list, function (method) { %>
		/**
		 * <%= method %> method.
		 */
		<%= method %>() {
		}
		<% }) %> <% } %>
	}

	angular.module('<%= moduleNameVar%>')
		.controller('<%= controllerNameVar %>', Controller);

})();